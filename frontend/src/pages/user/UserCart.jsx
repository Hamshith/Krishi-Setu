// We can reuse the FarmerCart entirely as it handles the exact same logic
import React from 'react';
import FarmerCart from '../farmer/FarmerCart';

const UserCart = () => {
  return (
    <FarmerCart />
  );
};

export default UserCart;
